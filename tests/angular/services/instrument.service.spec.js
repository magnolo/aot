ngDescribe({
    name: 'Test InstrumentService',
    modules: 'app',
    inject: 'InstrumentService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
