ngDescribe({
    name: 'Test autocomplete-list-inline component',
    modules: 'app',
    element: '<autocomplete-list-inline></autocomplete-list-inline>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
