ngDescribe({
    name: 'Test admin-header component',
    modules: 'app',
    element: '<admin-header></admin-header>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
