ngDescribe({
    name: 'Test document component',
    modules: 'app',
    element: '<document></document>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
