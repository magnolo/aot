ngDescribe({
    name: 'Test autocomplete-list component',
    modules: 'app',
    element: '<autocomplete-list></autocomplete-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
