ngDescribe({
    name: 'Test upload-wizard-header component',
    modules: 'app',
    element: '<upload-wizard-header></upload-wizard-header>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
